name: Northstar pipeline
on:
  workflow_call:
    inputs:
      workdir:
        type: string
        default: '.'
      zones-json:
        type: string
        required: true
      adapter:
        description: 'Language adapter: js-ts | python'
        type: string
        default: 'js-ts'
      install-cmd:
        description: 'Command to install project deps (e.g. "npm ci" or "pip install -r requirements.txt")'
        type: string
        default: 'npm ci'
      test-cmd:
        description: 'Override the adapter default test command (empty = adapter default)'
        type: string
        default: ''
      coverage-cmd:
        description: 'Override the adapter default coverage command (empty = adapter default)'
        type: string
        default: ''
      coverage-min:
        type: string
        default: '80'
      coverage-mode:
        type: string
        default: 'no-decrease'
      node-version:
        type: string
        default: '18'
      python-version:
        type: string
        default: '3.11'
      engine:
        description: 'Fix-agent engine: stub (default, no LLM) | claude-code'
        type: string
        default: 'stub'
      secret-scan:
        description: 'Run the gitleaks secret-scan hard-block before anything else'
        type: boolean
        default: true

# Least privilege: only what promote-regression needs on the default branch.
permissions:
  contents: write
  pull-requests: write

concurrency:
  group: northstar-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gate:
    runs-on: ubuntu-latest
    # Expose whether the TEST suite failed (vs the coverage gate), so the fix
    # job only runs for real test failures — not coverage drops (a human/test
    # author handles those).
    outputs:
      suite-failed: ${{ steps.suite.outcome == 'failure' }}
      # Touched zones (space-separated) → the fix job claims the primary one so
      # concurrent fixes on the same zone can't collide (coordination substrate).
      zones: ${{ steps.detect.outputs.zones }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      # Secret-scan FIRST: a leak is a hard block (fails the job here, before the
      # suite runs) and NO fix-agent is invoked — the fix job keys off suite
      # failure, and the suite step never runs when this fails.
      - name: secret-scan (hard block; no agent)
        if: ${{ inputs.secret-scan }}
        uses: dmjohnsonintl/northstar-ci/actions/secret-scan@v0
        with:
          workdir: ${{ inputs.workdir }}
      # Node is always set up — Northstar's own CLIs (gate, normalize) run on it,
      # regardless of the project's language.
      - uses: actions/setup-node@v5
        with:
          node-version: ${{ inputs.node-version }}
      - if: ${{ inputs.adapter == 'python' }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python-version }}
      - name: Install project deps
        working-directory: ${{ inputs.workdir }}
        env:
          INSTALL_CMD: ${{ inputs.install-cmd }}
        run: eval "$INSTALL_CMD"
      # Full public action refs — local './' refs do NOT resolve when this
      # reusable workflow is called from another repo (they'd resolve against the
      # caller's checkout). Pinned to @v0, this repo's release tag.
      - name: detect-changes
        id: detect
        uses: dmjohnsonintl/northstar-ci/actions/detect-changes@v0
        with:
          zones-json: ${{ inputs.zones-json }}
          base-sha: ${{ github.event.pull_request.base.sha || github.event.before }}
          head-sha: ${{ github.sha }}
      # hot-area (advisory signal): the touched zones are where activity/risk is
      # concentrated. TTL 24h; GC expires it. Emitted to the step summary always,
      # and as a PR label when there's a PR.
      - name: signal — hot-area
        if: ${{ steps.detect.outputs.zones != '' }}
        uses: dmjohnsonintl/northstar-ci/actions/signal@v0
        with:
          type: hot-area
          zone: ${{ steps.detect.outputs.zones }}
          pr-number: ${{ github.event.pull_request.number }}
          ttl-seconds: '86400'
          role: detect
      - name: run-suite
        id: suite
        continue-on-error: true
        uses: dmjohnsonintl/northstar-ci/actions/run-suite@v0
        with:
          workdir: ${{ inputs.workdir }}
          adapter: ${{ inputs.adapter }}
          test-cmd: ${{ inputs.test-cmd }}
      # flaky (advisory signal): the suite failed then passed on retry. Quarantine
      # it — this run is treated as green (no fix-agent), but the flake is flagged.
      - name: signal — flaky
        if: ${{ steps.suite.outputs.flaky == 'true' }}
        uses: dmjohnsonintl/northstar-ci/actions/signal@v0
        with:
          type: flaky
          zone: ${{ steps.detect.outputs.zones }}
          pr-number: ${{ github.event.pull_request.number }}
          ttl-seconds: '86400'
          role: run-suite
      # Coverage + promotion only make sense when the suite passed. continue-on-error
      # so we can deposit a coverage-gap SIGNAL on a gap, then re-assert the block.
      - name: coverage-gate
        if: ${{ steps.suite.outcome == 'success' }}
        id: gate
        continue-on-error: true
        uses: dmjohnsonintl/northstar-ci/actions/coverage-gate@v0
        with:
          workdir: ${{ inputs.workdir }}
          adapter: ${{ inputs.adapter }}
          coverage-cmd: ${{ inputs.coverage-cmd }}
          min: ${{ inputs.coverage-min }}
          mode: ${{ inputs.coverage-mode }}
      # coverage-gap (advisory signal) → feeds author-tests / operator readout.
      - name: signal — coverage-gap
        if: ${{ steps.gate.outcome == 'failure' }}
        uses: dmjohnsonintl/northstar-ci/actions/signal@v0
        with:
          type: coverage-gap
          zone: ${{ steps.detect.outputs.zones }}
          pr-number: ${{ github.event.pull_request.number }}
          ttl-seconds: '86400'
          role: coverage-gate
      # Only promote when BOTH the suite AND the coverage gate passed. (The gate
      # is continue-on-error above so we could signal on a gap; without this extra
      # gate.outcome guard, a coverage drop would no longer skip promotion.)
      - name: promote-regression (default branch only)
        if: ${{ steps.suite.outcome == 'success' && steps.gate.outcome == 'success' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch) }}
        uses: dmjohnsonintl/northstar-ci/actions/promote-regression@v0
        with:
          workdir: ${{ inputs.workdir }}
          next-baseline: ${{ steps.gate.outputs.next-baseline }}
      # Fail the job when the suite failed (run-suite is continue-on-error above
      # so we can branch on it, so re-assert the failure for the check status).
      - name: Fail if the test suite failed
        if: ${{ steps.suite.outcome == 'failure' }}
        run: |
          echo "::error::test suite failed"
          exit 1
      # Re-assert the coverage block (gate is continue-on-error above so we could
      # deposit the coverage-gap signal). A coverage gap blocks but does NOT set
      # suite-failed, so it never routes to the fix-agent — a human handles it.
      - name: Fail if the coverage gate failed
        if: ${{ steps.gate.outcome == 'failure' }}
        run: |
          echo "::error::coverage gate failed"
          exit 1

  # Single-attempt fix-agent: runs ONLY on a real test-suite failure (not a
  # coverage drop). Opens a PR on success, labels ns:needs-human on exhaustion.
  # Never auto-merges.
  fix:
    needs: gate
    # always() so this evaluates even though the gate job failed; then run ONLY
    # when the failure was a real test-suite failure (not a coverage drop).
    if: ${{ always() && needs.gate.outputs.suite-failed == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
          ref: ${{ github.head_ref || github.ref_name }}
      - uses: actions/setup-node@v5
        with:
          node-version: ${{ inputs.node-version }}
      - if: ${{ inputs.adapter == 'python' }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python-version }}
      - name: Install project deps
        working-directory: ${{ inputs.workdir }}
        env:
          INSTALL_CMD: ${{ inputs.install-cmd }}
        run: eval "$INSTALL_CMD"

      # route-failure: pick the primary failing zone.
      - name: Resolve the primary zone
        id: zone
        env:
          ZONES: ${{ needs.gate.outputs.zones }}
        run: |
          Z="${ZONES%% *}"          # first touched zone
          [ -z "$Z" ] && Z="repo"   # fallback when nothing mapped
          echo "name=$Z" >> "$GITHUB_OUTPUT"
          echo "[northstar] routing fix to zone: $Z"

      # Acquire an ENFORCEMENT CLAIM on the zone before fixing. Atomic: if another
      # run already holds ns/claim/<zone>, this returns acquired=false and we
      # queue (skip) instead of colliding. Released unconditionally at the end.
      - name: route-failure — acquire zone claim
        id: claim
        uses: dmjohnsonintl/northstar-ci/actions/claim@v0
        with:
          mode: acquire
          zone: ${{ steps.zone.outputs.name }}
          role: fixer
          ttl-seconds: '3600'

      - name: Zone already claimed → queue (no collision)
        if: ${{ steps.claim.outputs.acquired != 'true' }}
        run: echo "::notice::zone '${{ steps.zone.outputs.name }}' is held by ${{ steps.claim.outputs.holder }} — another run is fixing it; queuing this one."

      - name: Capture the failing-test log
        if: ${{ steps.claim.outputs.acquired == 'true' }}
        continue-on-error: true
        uses: dmjohnsonintl/northstar-ci/actions/run-suite@v0
        with:
          workdir: ${{ inputs.workdir }}
          adapter: ${{ inputs.adapter }}
          test-cmd: ${{ inputs.test-cmd }}
          retries: '0'   # deterministic failure already established; capture it once
      - name: fix-agent
        if: ${{ steps.claim.outputs.acquired == 'true' }}
        uses: dmjohnsonintl/northstar-ci/actions/fix-agent@v0
        with:
          workdir: ${{ inputs.workdir }}
          adapter: ${{ inputs.adapter }}
          test-cmd: ${{ inputs.test-cmd }}
          engine: ${{ inputs.engine }}
          failing-log: ${{ inputs.workdir }}/artifacts/test.log
          anthropic-api-key: ${{ secrets.ANTHROPIC_API_KEY }}

      # Mortality: release the claim whether the fix went green, escalated, or
      # errored — so the zone is freed immediately (GC is only the crash backstop).
      - name: Release the zone claim
        if: ${{ always() && steps.claim.outputs.acquired == 'true' }}
        uses: dmjohnsonintl/northstar-ci/actions/claim@v0
        with:
          mode: release
          zone: ${{ steps.zone.outputs.name }}
